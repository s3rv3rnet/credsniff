# credsniff documentation

Documentation intended for **users and contributors**. Everything here is public.

| Path | Contents |
|---|---|
| `adr/` | Architecture Decision Records — why key design choices were made |
| `rules.md` | The built-in rule pack, and how to write custom rules *(Step 11)* |
| `usage.md` | CLI reference and CI / pre-commit recipes *(Step 9)* |

> Private working notes (the build blueprint and per-step journal) live in `docs/`,
> which is git-ignored on purpose.
